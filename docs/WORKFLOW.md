# Maintenance workflow

Scout is temporarily a static under-construction page.

Run:

    python3 scripts/validate.py
    python3 scripts/build.py

before opening a pull request. A merge to `master` deploys only the approved placeholder artifact to GitHub Pages.

Do not use Scout to host or display the private Career queue.
